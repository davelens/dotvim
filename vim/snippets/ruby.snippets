snippet it
	it '${1}' do
		${2}
	end

snippet specfile
	require 'rails_helper'

	describe ${1} do
		let(:klass) { described_class.to_s.underscore.to_sym }

		it '#respond_to?' do
			expect(subject).to respond_to?(${2})
		end
	end
