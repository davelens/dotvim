-- NOTE: This config still uses the "old" nvim-treesitter 1.0 API (master
-- branch), but still works standalone regardless for now.
--
-- The old API using require('nvim-treesitter.configs').setup() is deprecated.
return {
  'RRethy/nvim-treesitter-endwise',
  lazy = true,
}
